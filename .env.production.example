# Production Environment Configuration
# Phase 11: Performance, DevOps & Deployment
# 
# Copy this file to .env and update with your production values
# DO NOT commit .env file to version control

# Database
DB_PASSWORD=your-secure-database-password-here

# JWT Secrets (minimum 32 characters each)
JWT_SECRET=your-very-secure-jwt-secret-key-minimum-32-characters
JWT_REFRESH_SECRET=your-very-secure-jwt-refresh-secret-key-minimum-32-characters

# CORS (Frontend URL)
CORS_ORIGIN=https://your-domain.com

# Port (default: 80)
PORT=80

# Optional: External database URL (if not using docker-compose database)
# DATABASE_URL=postgresql://user:password@host:5432/gramps

# Optional: External Redis URL (if not using docker-compose redis)
# REDIS_URL=redis://host:6379
